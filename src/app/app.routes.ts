import { Routes } from '@angular/router';
import { StadepladserComponent } from './pages/stadepladser/stadepladser.component';

export const routes: Routes = [
    {path: '', component: StadepladserComponent},
];
